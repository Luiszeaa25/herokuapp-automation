{
  "name": "herokuapp-automation",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "cucumber-js --config config/cucumber.cjs",
    "test:smoke": "cucumber-js --config config/cucumber.cjs --tags \"@smoke\"",
    "test:sanity": "cucumber-js --config config/cucumber.cjs --tags \"@sanity\"",
    "test:regression": "cucumber-js --config config/cucumber.cjs --tags \"@regression\"",
    "test:env:prod": "ENV=prod cucumber-js --config config/cucumber.cjs",
    "test:env:staging": "ENV=staging cucumber-js --config config/cucumber.cjs",
    "report:generate": "npx allure generate allure-results --clean -o allure-report",
    "report:open": "allure open allure-report",
    "report:env": "mkdir -p allure-results && cp src/helper/report/environment.properties allure-results/ && cp src/helper/report/categories.json allure-results/",
    "report": "npm run report:env && npm run report:generate && npm run report:open",
    "clean": "rm -rf allure-results allure-report",
    "test:smoke:report": "npm run clean && npm run test:smoke && npm run report",
    "test:sanity:report": "npm run clean && npm run test:sanity && npm run report",
    "test:regression:report": "npm run clean && npm run test:regression && npm run report",
    "test:report": "npm run clean && npm test && npm run report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cucumber/cucumber": "^12.8.1",
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.2",
    "@types/winston": "^2.4.4",
    "allure-commandline": "^2.38.1",
    "allure-cucumberjs": "^3.7.1",
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "winston": "^3.19.0"
  }
}
